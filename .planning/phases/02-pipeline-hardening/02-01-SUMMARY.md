---
phase: 02-pipeline-hardening
plan: "01"
subsystem: database, infra
tags: [structlog, alembic, sqlalchemy, pipeline, logging]

# Dependency graph
requires:
  - phase: 01-foundation
    provides: SQLAlchemy Base, Alembic migration chain (0002), IngestionLog model, app.py lifespan
provides:
  - PipelineRun SQLAlchemy model (grantflow/models.py) with all 12 fields
  - Alembic migration 0003 creating pipeline_runs table
  - configure_structlog() - JSON in production, colored key-value in development
  - bind_source_logger(source) - returns bound structlog logger for ingest modules
  - grantflow/pipeline/ package as logging infrastructure base
affects:
  - 02-pipeline-hardening (Plans 02-05 all use PipelineRun and bind_source_logger)
  - stale-data monitoring (Plan 05 reads pipeline_runs table)

# Tech tracking
tech-stack:
  added: [structlog==25.5.0]
  patterns:
    - structlog with stdlib bridge (logging.basicConfig routes stdlib into structlog)
    - Env-driven renderer selection (JSONRenderer for production, ConsoleRenderer for dev)
    - bind_source_logger() pattern for per-ingest bound context

key-files:
  created:
    - grantflow/pipeline/__init__.py
    - grantflow/pipeline/logging.py
    - alembic/versions/0003_pipeline_run_table.py
  modified:
    - grantflow/models.py
    - grantflow/app.py
    - grantflow/ingest/run_all.py

key-decisions:
  - "Wrote 0003 migration manually (not autogenerated) — autogenerate detected SQLite FTS5 virtual table drift and search_vector column as spurious changes; clean manual migration avoids scope leak"
  - "Stamped alembic at 0002 — DB had tables applied directly (init_db) but alembic_version table was empty; stamp required before autogenerate could run"
  - "structlog 25.5.0 selected (latest) — plan required >=24.0, no pinning needed at this stage"

patterns-established:
  - "Pipeline logging: always call configure_structlog() at startup before any other init"
  - "Ingest loggers: use bind_source_logger('source_name') to get source-tagged logger"
  - "PipelineRun.error_message is Text (unbounded) unlike IngestionLog.error which truncates at 100 chars"

requirements-completed: [PIPE-06]

# Metrics
duration: 15min
completed: 2026-03-24
---

# Phase 2 Plan 01: Pipeline Hardening Foundation Summary

**structlog JSON/dev logging infrastructure + PipelineRun table (12-field SQLAlchemy model + Alembic migration 0003) as shared foundation for all Phase 2 ingestors**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-03-24T17:45:00Z
- **Completed:** 2026-03-24T18:00:00Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments

- PipelineRun model added to grantflow/models.py with all required fields (id, source, run_type, status, started_at, completed_at, records_processed, records_added, records_updated, records_failed, error_message, extra)
- Alembic migration 0003 written and applied — pipeline_runs table live in DB
- structlog 25.5.0 installed; configure_structlog() and bind_source_logger() live in grantflow/pipeline/logging.py
- configure_structlog() wired into app.py lifespan and run_all.py main() replacing logging.basicConfig()
- 5/5 Phase 1 tests still pass (zero regressions)

## Task Commits

Each task was committed atomically:

1. **Task 1: Add PipelineRun model and Alembic migration 0003** - `f26ec0b` (feat)
2. **Task 2: Add structlog and pipeline logging module** - `f691d82` (feat)

**Plan metadata:** (docs commit follows)

## Files Created/Modified

- `grantflow/models.py` - Added PipelineRun class after IngestionLog (IngestionLog untouched)
- `alembic/versions/0003_pipeline_run_table.py` - Migration: op.create_table('pipeline_runs'), down_revision='0002'
- `grantflow/pipeline/__init__.py` - Empty package init
- `grantflow/pipeline/logging.py` - configure_structlog() and bind_source_logger() implementations
- `grantflow/app.py` - Added configure_structlog() call at top of lifespan, before init_db()
- `grantflow/ingest/run_all.py` - Replaced logging.basicConfig() with configure_structlog() in main()

## Decisions Made

- **Manual migration over autogenerate:** Alembic autogenerate detected spurious SQLite FTS5 virtual tables (opportunities_fts_*) and a missing search_vector column as drift. These are pre-existing schema inconsistencies from Phase 1 (init_db applied tables directly before alembic was fully wired). Wrote 0003 manually with only the pipeline_runs create_table to avoid importing unrelated changes.
- **DB stamp at 0002:** alembic_version table was empty despite tables existing. Stamped at 0002 (Phase 1 head) before generating 0003.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Stamped alembic version before migration generation**
- **Found during:** Task 1 (Alembic migration)
- **Issue:** `alembic revision --autogenerate` failed with "Target database is not up to date" — alembic_version table was empty
- **Fix:** Ran `uv run alembic stamp 0002` to register existing DB state, then generated migration
- **Files modified:** alembic_version table in grantflow.db (no file change)
- **Verification:** `alembic current` showed `0002 (head)` before generation
- **Committed in:** f26ec0b (Task 1 commit)

**2. [Rule 3 - Blocking] Replaced autogenerated migration with clean manual file**
- **Found during:** Task 1 (reviewing generated migration)
- **Issue:** Autogenerated file included `op.drop_table()` for 5 FTS virtual tables and `op.add_column()` for search_vector — unrelated to this plan's scope, would break the DB
- **Fix:** Deleted autogenerated file, wrote 0003_pipeline_run_table.py manually with only the pipeline_runs table
- **Files modified:** alembic/versions/0003_pipeline_run_table.py
- **Verification:** Migration applied cleanly (0002 -> 0003); PipelineRun importable; alembic history shows 0003 at head
- **Committed in:** f26ec0b (Task 1 commit)

---

**Total deviations:** 2 auto-fixed (both Rule 3 - blocking)
**Impact on plan:** Both fixes required to unblock task completion. No scope creep — migration contains only what the plan specified.

## Issues Encountered

Pre-existing schema drift between the SQLite DB and alembic tracking (FTS5 virtual tables, search_vector column) will continue to cause `alembic check` to fail. These are out-of-scope for this plan and should be addressed in a dedicated migration cleanup task.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- PipelineRun model and pipeline_runs table are ready for Plans 02-04 ingestors to write run records
- bind_source_logger() available for all ingest modules to use immediately
- configure_structlog() called at both app startup and CLI entry — no additional wiring needed
- Blocker note: alembic check drift (FTS5 tables + search_vector) should be stamped/resolved before any future autogenerate migrations

---
*Phase: 02-pipeline-hardening*
*Completed: 2026-03-24*

## Self-Check: PASSED

All files confirmed present, all commits confirmed in git log.
