---
phase: 07-gtm-enrichment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - grantflow/web/routes.py
  - grantflow/analytics/__init__.py
  - grantflow/analytics/middleware.py
  - grantflow/models.py
  - grantflow/app.py
  - templates/landing.html
  - templates/pricing.html
  - templates/playground.html
  - alembic/versions/0007_add_api_events.py
  - scripts/seed_demo_key.py
  - tests/test_gtm_pages.py
  - tests/test_analytics.py
autonomous: true
requirements:
  - GTM-01
  - GTM-02
  - GTM-03
  - GTM-04

must_haves:
  truths:
    - "Visitor to / sees a landing page with value proposition and link to /pricing"
    - "Visitor to /pricing sees coverage-based tier display (not API call volume)"
    - "Visitor to /playground can run a live API query without creating an account"
    - "Every API request creates a row in api_events with path, method, status_code, duration_ms"
  artifacts:
    - path: "templates/landing.html"
      provides: "Landing page with value prop, CTA to pricing and search"
      min_lines: 40
    - path: "templates/pricing.html"
      provides: "Pricing page with coverage-based tiers"
      min_lines: 30
    - path: "templates/playground.html"
      provides: "Interactive API playground with vanilla JS"
      min_lines: 50
    - path: "grantflow/analytics/middleware.py"
      provides: "Non-blocking analytics event recording"
      exports: ["record_api_event"]
    - path: "grantflow/models.py"
      provides: "ApiEvent ORM model"
      contains: "class ApiEvent"
    - path: "alembic/versions/0007_add_api_events.py"
      provides: "api_events table migration"
    - path: "scripts/seed_demo_key.py"
      provides: "Idempotent demo API key provisioner"
  key_links:
    - from: "grantflow/app.py"
      to: "grantflow/analytics/middleware.py"
      via: "@app.middleware('http') analytics capture"
      pattern: "analytics_middleware|record_api_event"
    - from: "grantflow/web/routes.py"
      to: "templates/playground.html"
      via: "GRANTFLOW_DEMO_API_KEY env var injected at render"
      pattern: "GRANTFLOW_DEMO_API_KEY|demo_api_key"
    - from: "grantflow/web/routes.py"
      to: "templates/landing.html"
      via: "/ route renders landing instead of redirect"
      pattern: "landing.html"
---

<objective>
Build the GTM web pages (landing, pricing, playground) and usage analytics middleware so the product has a public face and tracks all API usage.

Purpose: Visitors can understand the product, see pricing, and try the API without signing up. Every API hit is tracked for usage analytics.
Output: Three new Jinja2 templates, analytics middleware with api_events table, demo key seed script, and tests.
</objective>

<execution_context>
@/home/jeff/.claude/get-shit-done/workflows/execute-plan.md
@/home/jeff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-gtm-enrichment/07-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From grantflow/web/routes.py:
```python
router = APIRouter()
templates = Jinja2Templates(directory=str(BASE_DIR / "templates"))

@router.get("/")
def index():
    return RedirectResponse(url="/search", status_code=302)
```

From grantflow/models.py:
```python
class Base(DeclarativeBase): pass
class Opportunity(Base):
    __tablename__ = "opportunities"
class ApiKey(Base):
    __tablename__ = "api_keys"
```

From grantflow/app.py:
```python
from apscheduler.schedulers.asyncio import AsyncIOScheduler
scheduler = AsyncIOScheduler()
# Middleware is registered via @app.middleware("http") or app.add_middleware()
# app = FastAPI(lifespan=lifespan, ...)
```

From grantflow/database.py:
```python
# SessionLocal = sessionmaker(...)
# get_db() is the FastAPI Depends() provider
```

From grantflow/config.py:
```python
BASE_DIR = Path(__file__).resolve().parent.parent
DATABASE_URL = os.getenv("GRANTFLOW_DATABASE_URL", "sqlite:///...")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GTM pages, demo key seed, and analytics DB table</name>
  <files>
    grantflow/models.py,
    grantflow/analytics/__init__.py,
    grantflow/analytics/middleware.py,
    grantflow/web/routes.py,
    alembic/versions/0007_add_api_events.py,
    scripts/seed_demo_key.py,
    templates/landing.html,
    templates/pricing.html,
    templates/playground.html,
    tests/test_gtm_pages.py,
    tests/test_analytics.py
  </files>
  <behavior>
    - test_landing_page: GET / returns 200 with "GrantFlow" and link to /pricing in body
    - test_pricing_page: GET /pricing returns 200 with tier names (Free, Starter, Growth)
    - test_playground_page: GET /playground returns 200 with no 500 error (demo key injected or gracefully absent)
    - test_event_recorded: After a GET /api/v1/opportunities/search request, an api_events row exists with correct path and status_code
    - test_analytics_no_latency_impact: Analytics middleware does not add measurable latency (response returns before DB write)
  </behavior>
  <action>
    1. Add ApiEvent model to grantflow/models.py (id, ts, path, method, api_key_prefix, query_string, status_code, duration_ms). Place after ApiKey class.

    2. Create Alembic migration 0007_add_api_events.py: CREATE TABLE api_events with columns matching the model. Add indexes on ts and api_key_prefix. Use `revision='0007'`, `down_revision='0005'` (verify current head first with `alembic heads`).

    3. Create grantflow/analytics/__init__.py (empty) and grantflow/analytics/middleware.py:
       - `record_api_event(**kwargs)` function that creates a SessionLocal(), inserts one ApiEvent row, commits, and closes the session. Wrap in try/except so analytics never crashes the app.
       - `setup_analytics_middleware(app)` function that registers `@app.middleware("http")` using the decorator pattern (NOT BaseHTTPMiddleware). Capture start time before `call_next`, compute duration_ms after, attach `response.background = BackgroundTask(record_api_event, ...)`. Extract api_key_prefix from X-API-Key header (first 8 chars). Skip recording for static file paths (/static/).

    4. Wire analytics into grantflow/app.py: Import and call `setup_analytics_middleware(app)` after CORS middleware setup. Do NOT use BaseHTTPMiddleware class.

    5. Update grantflow/web/routes.py:
       - Replace the `GET /` redirect with a landing page route: query total opportunity count from DB, render templates/landing.html with total_opps context.
       - Add `GET /pricing` route rendering templates/pricing.html (no DB needed).
       - Add `GET /playground` route rendering templates/playground.html with demo_api_key from `os.getenv("GRANTFLOW_DEMO_API_KEY", "")`.

    6. Create templates/landing.html extending base.html:
       - Hero section: "Government Grant Data, Instantly Accessible" (or similar)
       - Value propositions: unified data from federal + state sources, clean normalized data, simple API
       - Social proof: "{{ total_opps|default(0) }} opportunities indexed" dynamic count
       - CTAs: link to /search ("Explore Grants"), link to /pricing ("See Pricing"), link to /playground ("Try the API")

    7. Create templates/pricing.html extending base.html:
       - Three tiers based on DATA COVERAGE (not API call volume per project decision):
         - Free: Federal grants only, 1000 req/day
         - Starter: Federal + 5 states, 10K req/day
         - Growth: All sources + bulk export, 100K req/day
       - No Stripe integration, no payment buttons (display only per project decision)

    8. Create templates/playground.html extending base.html:
       - Text input for search query, "Run Query" button
       - Vanilla JS fetch() to /api/v1/opportunities/search?q={query}&per_page=5 with X-API-Key header set to demo key injected from server
       - Pre-formatted output area showing JSON response
       - Graceful handling when GRANTFLOW_DEMO_API_KEY is not set (show message: "Demo key not configured")

    9. Create scripts/seed_demo_key.py: Idempotent script that checks for existing demo key (name="playground-demo"), creates one if missing using the same hash pattern as existing key creation (gf_ prefix + secrets.token_urlsafe(32), SHA-256 hash stored). Prints the plaintext key. Uses SessionLocal directly. Run with `uv run python scripts/seed_demo_key.py`.

    10. Write tests/test_gtm_pages.py:
        - test_landing_page: GET / via TestClient, assert 200, assert "GrantFlow" in body, assert "/pricing" href in body
        - test_pricing_page: GET /pricing, assert 200, assert "Free" and "Starter" and "Growth" in body
        - test_playground_page: GET /playground, assert 200, assert no 500

    11. Write tests/test_analytics.py:
        - test_event_recorded: Make a request via TestClient, then query api_events table to verify a row was created with matching path and status_code. Use the test DB session from conftest.py.

    Run tests RED first, then implement, then GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/test_gtm_pages.py tests/test_analytics.py -x -v</automated>
  </verify>
  <done>
    - GET / returns landing page with value prop (not redirect to /search)
    - GET /pricing returns pricing page with Free/Starter/Growth tiers
    - GET /playground returns interactive API playground
    - API requests generate api_events rows via non-blocking background task
    - Demo key seed script provisions a playground-demo key idempotently
    - All tests pass
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_gtm_pages.py tests/test_analytics.py -x -v` all pass
- `uv run pytest tests/ -x` full suite still green (no regressions)
- `uv run alembic upgrade head` runs without errors (migration 0007 applies cleanly)
</verification>

<success_criteria>
- Landing page at / shows product value proposition with link to pricing
- Pricing page at /pricing shows three coverage-based tiers
- Playground at /playground renders with demo key injection (or graceful fallback)
- Every API request is tracked in api_events table via non-blocking middleware
- All new and existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-gtm-enrichment/07-01-SUMMARY.md`
</output>
