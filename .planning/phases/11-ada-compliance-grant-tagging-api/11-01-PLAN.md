---
phase: 11-ada-compliance-grant-tagging-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - grantflow/pipeline/ada_tagger.py
  - tests/test_ada_compliance.py
autonomous: true
requirements:
  - ADA-01

must_haves:
  truths:
    - "ADA keyword matcher correctly identifies ADA-related grants and rejects false positives"
    - "Backfill script tags all matching records in topic_tags column with ada-compliance"
    - "Backfill is idempotent — re-running does not duplicate the ada-compliance tag"
    - "Malformed topic_tags JSON is handled gracefully without crashing"
  artifacts:
    - path: "grantflow/pipeline/ada_tagger.py"
      provides: "ADA keyword matching + backfill CLI script"
      exports: ["run_ada_backfill", "_is_ada_match", "_parse_tags", "ADA_TITLE_KEYWORDS", "ADA_DESC_KEYWORDS", "ADA_AGENCY_KEYWORDS"]
      min_lines: 80
    - path: "tests/test_ada_compliance.py"
      provides: "Unit tests for keyword matching + backfill logic"
      contains: "test_keyword_matching"
      min_lines: 60
  key_links:
    - from: "grantflow/pipeline/ada_tagger.py"
      to: "opportunities table"
      via: "raw SQL SELECT + UPDATE (not ORM)"
      pattern: "text\\(\"SELECT id, title, description"
    - from: "grantflow/pipeline/ada_tagger.py"
      to: "topic_tags column"
      via: "json.dumps to serialize tag list"
      pattern: "json\\.dumps\\(tags\\)"
---

<objective>
Create the ADA keyword matching module and backfill CLI script that tags all ADA-related grants in the existing topic_tags column.

Purpose: ADA-01 requires an ada_tags mechanism populated for all grants matching ADA/accessibility keyword criteria. The existing topic_tags Text column (JSON string) is reused — no migration needed.
Output: grantflow/pipeline/ada_tagger.py (CLI-callable) + unit tests proving keyword accuracy and backfill correctness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ada-compliance-grant-tagging-api/11-CONTEXT.md
@.planning/phases/11-ada-compliance-grant-tagging-api/11-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From tests/conftest.py:
```python
@pytest.fixture(scope="session")
def test_engine():
    engine = create_engine("sqlite:///./test_grantflow.db", connect_args={"check_same_thread": False})
    Base.metadata.create_all(bind=engine)
    yield engine

@pytest.fixture(scope="function")
def db_session(test_engine):
    TestingSessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=test_engine)
    session = TestingSessionLocal()
    yield session
    session.rollback()
    session.close()

@pytest.fixture(scope="function")
def client(db_session):
    def override_get_db():
        yield db_session
    app.dependency_overrides[get_db] = override_get_db
    with TestClient(app) as c:
        yield c
    app.dependency_overrides.clear()
```

From grantflow/models.py (relevant column):
```python
topic_tags = Column(Text, nullable=True)  # JSON string: '["health", "research"]' or None
```

From grantflow/database.py:
```python
from sqlalchemy.orm import sessionmaker, Session
SessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)
def get_db():
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ADA tagger module with TDD</name>
  <files>grantflow/pipeline/ada_tagger.py, tests/test_ada_compliance.py</files>
  <read_first>
    - tests/conftest.py (test fixtures: db_session, client, test_engine)
    - grantflow/models.py (Opportunity model, topic_tags column definition)
    - grantflow/database.py (SessionLocal import for CLI __main__ block)
    - grantflow/pipeline/ada_tagger.py (will not exist yet — confirm absence)
  </read_first>
  <behavior>
    - test_keyword_matching_true: _is_ada_match returns True for "Americans with Disabilities Act" in title, "ADA compliance" in title, "Federal Transit Administration" in agency_name, "paratransit" in description, "All Stations Accessibility" in title, "Section 504" in description, "wheelchair" in title, "curb cut" in description
    - test_keyword_matching_false: _is_ada_match returns False for "ADAPTATION program" in title (bare ADA substring), "Adams County" in title, "NADAC pricing" in title, "academic" in description, empty strings, None fields
    - test_parse_tags_none: _parse_tags(None) returns []
    - test_parse_tags_valid: _parse_tags('["health", "research"]') returns ["health", "research"]
    - test_parse_tags_malformed: _parse_tags('not json{') returns []
    - test_parse_tags_non_list: _parse_tags('"just a string"') returns []
    - test_backfill_tags_matching_records: Insert 3 rows (1 ADA-matching title, 1 ADA-matching agency, 1 non-matching). run_ada_backfill tags exactly 2 rows. topic_tags for tagged rows contains '["ada-compliance"]'. Non-matching row topic_tags is still None.
    - test_backfill_idempotent: Insert 1 ADA-matching row, run backfill twice. topic_tags contains exactly one "ada-compliance" entry (not duplicated).
    - test_backfill_preserves_existing_tags: Insert 1 ADA-matching row with topic_tags='["health"]'. After backfill, topic_tags is '["health", "ada-compliance"]'.
    - test_backfill_malformed_tags: Insert 1 ADA-matching row with topic_tags='broken{json'. After backfill, topic_tags is '["ada-compliance"]' (graceful recovery).
  </behavior>
  <action>
    RED phase: Create tests/test_ada_compliance.py with all unit tests listed in behavior block. Tests import from grantflow.pipeline.ada_tagger. Use db_session fixture from conftest.py. Insert test rows using raw SQL: `db_session.execute(text("INSERT INTO opportunities (id, title, description, agency_name, topic_tags, source) VALUES (:id, :title, :desc, :agency, :tags, :src)"), {...})` followed by `db_session.flush()`. Run `uv run pytest tests/test_ada_compliance.py -x` — all tests MUST FAIL (ImportError or AssertionError).

    GREEN phase: Create grantflow/pipeline/ada_tagger.py with:

    1. ADA_TITLE_KEYWORDS list (all lowercase): "americans with disabilities act", "ada compliance", "ada remediat", "ada transition", "ada access", "ada standards", "accessibility compliance", "transit accessibility", "accessible transit", "paratransit", "wheelchair", "curb cut", "pedestrian accessibility", "sidewalk accessibility", "all stations accessibility", "station accessibility", "rail accessibility", "bus accessibility", "accessible transportation", "disability remediation", "accessible facilities", "disability infrastructure", "fta accessibility", "disability access"

    2. ADA_DESC_KEYWORDS list (all lowercase): "americans with disabilities act", "ada compliance", "ada transition plan", "paratransit", "section 504", "wheelchair", "curb cut", "all stations accessibility", "transit accessibility", "accessible transit", "rehabilitation act", "disability remediation", "accessible facilities"

    3. ADA_AGENCY_KEYWORDS list (all lowercase): "federal transit administration"
       NOTE: Do NOT include "Office of Special Education" per RESEARCH.md recommendation — it would tag IDEA/education grants, not ADA infrastructure compliance.

    4. _is_ada_match(title: str | None, description: str | None, agency_name: str | None) -> bool:
       Lowercase all three fields (or "" if None), return True if any keyword in the respective list appears as a substring in the respective field.

    5. _parse_tags(raw: str | None) -> list[str]:
       None -> []. Try json.loads; if result is list return it, else []. On JSONDecodeError/ValueError -> [].

    6. run_ada_backfill(db=None) -> int:
       If db is None, create SessionLocal() and set close_db=True.
       Execute raw SQL: `SELECT id, title, description, agency_name, topic_tags FROM opportunities`
       For each row: if _is_ada_match, parse existing tags, append "ada-compliance" if not present, UPDATE via raw SQL.
       Commit once after loop. Return count of updated rows. Close db in finally block if close_db.

    7. `if __name__ == "__main__":` block that calls run_ada_backfill() and prints count.

    Run `uv run pytest tests/test_ada_compliance.py -x` — all tests MUST PASS.
  </action>
  <verify>
    <automated>uv run pytest tests/test_ada_compliance.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grantflow/pipeline/ada_tagger.py exists and contains `def run_ada_backfill(`
    - grantflow/pipeline/ada_tagger.py contains `def _is_ada_match(`
    - grantflow/pipeline/ada_tagger.py contains `def _parse_tags(`
    - grantflow/pipeline/ada_tagger.py contains `ADA_TITLE_KEYWORDS`
    - grantflow/pipeline/ada_tagger.py contains `ADA_DESC_KEYWORDS`
    - grantflow/pipeline/ada_tagger.py contains `ADA_AGENCY_KEYWORDS`
    - grantflow/pipeline/ada_tagger.py contains `federal transit administration`
    - grantflow/pipeline/ada_tagger.py does NOT contain bare `"ada"` as a standalone keyword (grep for `"ada",` should return 0 matches — all keywords are contextually qualified)
    - grantflow/pipeline/ada_tagger.py contains `text("SELECT id, title, description, agency_name, topic_tags FROM opportunities")` (raw SQL, not ORM)
    - grantflow/pipeline/ada_tagger.py contains `json.dumps(tags)` for serialization
    - grantflow/pipeline/ada_tagger.py contains `if __name__ == "__main__":`
    - tests/test_ada_compliance.py contains `test_keyword_matching` and `test_backfill_tags_matching_records` and `test_backfill_idempotent`
    - `uv run pytest tests/test_ada_compliance.py -x` exits 0
  </acceptance_criteria>
  <done>
    _is_ada_match correctly identifies ADA grants (True for contextual keywords, False for false positives like "adaptation"/"Adams"). run_ada_backfill tags matching rows, is idempotent, handles malformed JSON. All 10 unit tests pass.
  </done>
</task>

</tasks>

<verification>
uv run pytest tests/test_ada_compliance.py -x -v
</verification>

<success_criteria>
- ada_tagger.py exists with keyword matching + backfill logic using raw SQL
- All unit tests pass (keyword matching true/false, backfill tagging, idempotency, malformed JSON)
- No bare "ADA" substring in keyword lists (false positive prevention)
- CLI callable via `uv run python -m grantflow.pipeline.ada_tagger`
</success_criteria>

<output>
After completion, create `.planning/phases/11-ada-compliance-grant-tagging-api/11-01-SUMMARY.md`
</output>
