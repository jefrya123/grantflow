---
phase: 12-fund-your-fix-web-page-seo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - grantflow/web/routes.py
  - templates/fund_your_fix.html
  - templates/fund_your_fix_widget.html
autonomous: true
requirements:
  - ADA-04
  - ADA-05
  - ADA-06

must_haves:
  truths:
    - "/fund-your-fix renders ADA grants sorted by deadline with title, deadline, award range, agency, and apply link"
    - "/fund-your-fix?municipality=boston shows relevant grants for that municipality with a banner"
    - "DOT FTA All Stations Access grant is pinned as featured when present"
    - "/ada-grants returns 301 redirect to /fund-your-fix"
    - "/fund-your-fix/widget returns minimal HTML fragment with top 5 ADA grants"
    - "og:image meta tag is present in the page head"
    - "JSON-LD ItemList structured data is present"
  artifacts:
    - path: "grantflow/web/routes.py"
      provides: "fund_your_fix_page with municipality filter, featured grant pinning, /ada-grants redirect, /fund-your-fix/widget"
      contains: "def fund_your_fix_page"
    - path: "templates/fund_your_fix.html"
      provides: "og:image meta tag, municipality banner"
      contains: "og:image"
    - path: "templates/fund_your_fix_widget.html"
      provides: "Standalone HTML fragment for iframe embedding"
  key_links:
    - from: "grantflow/web/routes.py"
      to: "templates/fund_your_fix.html"
      via: "TemplateResponse with municipality and featured_is_fta context vars"
      pattern: "featured_is_fta"
    - from: "grantflow/web/routes.py"
      to: "templates/fund_your_fix_widget.html"
      via: "TemplateResponse for widget endpoint"
      pattern: "fund_your_fix_widget.html"
---

<objective>
Add municipality filtering, featured FTA grant pinning, /ada-grants redirect, embeddable widget endpoint, and og:image meta tag to the /fund-your-fix page.

Purpose: Complete the Fund Your Fix web page so it meets all SEO and functionality requirements for public ADA grant discovery.
Output: Updated routes.py with 3 new/modified endpoints, updated fund_your_fix.html with og:image and municipality banner, new fund_your_fix_widget.html standalone template.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-fund-your-fix-web-page-seo/12-CONTEXT.md

<interfaces>
<!-- Key patterns the executor needs from existing code -->

From grantflow/web/routes.py (current fund_your_fix_page, lines 267-311):
```python
@router.get("/fund-your-fix")
def fund_your_fix_page(
    request: Request,
    page: int = Query(default=1, ge=1),
    per_page: int = Query(default=20, ge=1, le=100),
    db: Session = Depends(get_db),
):
    query = db.query(Opportunity).filter(
        Opportunity.topic_tags.ilike("%ada-compliance%")
    )
    # ... sorts by close_date asc nullslast
    featured = results[0] if results else None
    # ... passes featured, days_until_close to template
```

From grantflow/api/routes.py (municipality filter pattern, lines 190-216):
```python
if municipality:
    slug_term = f"%{municipality}%"
    muni_query = ada_query.filter(
        or_(
            Opportunity.eligible_applicants.ilike(slug_term),
            Opportunity.description.ilike(slug_term),
        )
    )
    if muni_query.count() > 0:
        ada_query = muni_query
    # else: fall through — return all ADA grants (fail-open)
```

From templates/fund_your_fix.html (head_extra block, lines 4-30):
```html
{% block head_extra %}
<meta name="description" content="...">
<meta property="og:title" content="Fund Your Fix — ADA Compliance Grants">
<meta property="og:description" content="...">
<meta property="og:type" content="website">
<meta name="twitter:card" content="summary">
<meta name="twitter:title" content="Fund Your Fix — ADA Compliance Grants">
<script type="application/ld+json">...</script>
{% endblock %}
```

Imports already in web/routes.py:
```python
from fastapi import APIRouter, Depends, Query, Request, HTTPException
from fastapi.templating import Jinja2Templates
from sqlalchemy.orm import Session
from sqlalchemy import func
from grantflow.models import Opportunity, Award
from grantflow.database import get_db
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add municipality filter, featured FTA pinning, /ada-grants redirect, and widget endpoint to routes.py</name>
  <files>grantflow/web/routes.py</files>
  <read_first>
    - grantflow/web/routes.py (current state of all routes)
    - grantflow/api/routes.py (lines 190-230 for municipality filter pattern)
  </read_first>
  <action>
Modify `grantflow/web/routes.py` with these changes:

1. **Add imports** at the top: `from sqlalchemy import or_` (add `or_` to the existing sqlalchemy import) and `from fastapi.responses import RedirectResponse`.

2. **Add /ada-grants redirect** — MUST be placed BEFORE the `/fund-your-fix` route (route ordering matters):
```python
@router.get("/ada-grants")
def ada_grants_redirect():
    return RedirectResponse("/fund-your-fix", status_code=301)
```

3. **Modify `fund_your_fix_page()`** to accept municipality param and pin FTA grant:
   - Add `municipality: str | None = Query(default=None)` parameter
   - After the base ada-compliance query, add municipality filter using the EXACT same fail-open pattern from the API endpoint:
     ```python
     if municipality:
         slug_term = f"%{municipality}%"
         muni_query = query.filter(
             or_(
                 Opportunity.eligible_applicants.ilike(slug_term),
                 Opportunity.description.ilike(slug_term),
             )
         )
         if muni_query.count() > 0:
             query = muni_query
     ```
   - For featured grant pinning, BEFORE selecting `featured = results[0]`, query specifically for FTA grant:
     ```python
     fta_grant = db.query(Opportunity).filter(
         Opportunity.topic_tags.ilike('%"ada-compliance"%'),
         Opportunity.title.ilike("%all stations%"),
     ).order_by(Opportunity.close_date.asc().nullslast()).first()
     featured = fta_grant if fta_grant else (results[0] if results else None)
     featured_is_fta = fta_grant is not None and featured == fta_grant
     ```
   - Add `municipality` and `featured_is_fta` to the template context dict.

4. **Add widget endpoint** AFTER the fund_your_fix_page route:
```python
@router.get("/fund-your-fix/widget")
def fund_your_fix_widget(
    request: Request,
    db: Session = Depends(get_db),
):
    results = (
        db.query(Opportunity)
        .filter(Opportunity.topic_tags.ilike('%"ada-compliance"%'))
        .order_by(Opportunity.close_date.asc().nullslast())
        .limit(5)
        .all()
    )
    return templates.TemplateResponse(
        request,
        "fund_your_fix_widget.html",
        context={"results": results},
    )
```

IMPORTANT: The `/fund-your-fix/widget` route MUST be registered BEFORE any `/{opportunity_id}` path-param route (it already will be since fund-your-fix routes are before /opportunity/{id}).
  </action>
  <verify>
    <automated>cd /home/jeff/Projects/grantflow && uv run python -c "from grantflow.web.routes import router; routes = [r.path for r in router.routes]; assert '/ada-grants' in routes; assert '/fund-your-fix' in routes; assert '/fund-your-fix/widget' in routes; print('All routes registered')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def ada_grants_redirect" grantflow/web/routes.py` returns 1
    - `grep -c "RedirectResponse" grantflow/web/routes.py` returns at least 1
    - `grep -c "municipality" grantflow/web/routes.py` returns at least 3 (param + filter + context)
    - `grep -c "featured_is_fta" grantflow/web/routes.py` returns at least 2 (assignment + context)
    - `grep -c "fund_your_fix_widget" grantflow/web/routes.py` returns at least 2 (function + template ref)
    - `grep "all stations" grantflow/web/routes.py` matches the ilike query for FTA pinning
    - `grep "status_code=301" grantflow/web/routes.py` confirms 301 redirect
    - `grep "or_" grantflow/web/routes.py` confirms or_ import is present
  </acceptance_criteria>
  <done>fund_your_fix_page accepts municipality param with fail-open filter, FTA grant is pinned as featured, /ada-grants redirects 301 to /fund-your-fix, /fund-your-fix/widget returns top 5 grants</done>
</task>

<task type="auto">
  <name>Task 2: Add og:image and municipality banner to template, create widget template</name>
  <files>templates/fund_your_fix.html, templates/fund_your_fix_widget.html</files>
  <read_first>
    - templates/fund_your_fix.html (current template)
    - templates/base.html (head_extra block pattern)
  </read_first>
  <action>
1. **Update `templates/fund_your_fix.html`**:

   a. Add `og:image` meta tag in the `head_extra` block, right after the existing `og:type` line:
   ```html
   <meta property="og:image" content="/static/og-fund-your-fix.png">
   ```

   b. Add municipality banner right after the hero-section closing `</div>` and before the `{% if featured %}` block:
   ```html
   {% if municipality %}
   <div style="background: #e8f4fd; border: 1px solid #b3d9f2; border-radius: 6px; padding: 0.75rem 1.25rem; margin-bottom: 1.5rem; color: #1a5276;">
       <strong>Showing grants for:</strong> {{ municipality }}
       <a href="/fund-your-fix" style="margin-left: 1rem; color: #2563a8;">Clear filter</a>
   </div>
   {% endif %}
   ```

   c. Update the featured grant header label to show "Featured: DOT FTA Grant" when `featured_is_fta` is true. Change the "Most Urgent Deadline" div to:
   ```html
   <div style="font-size: 0.8rem; font-weight: 700; color: #e55a00; text-transform: uppercase; letter-spacing: 0.05em; margin-bottom: 0.5rem;">
       {% if featured_is_fta %}Featured: DOT FTA Grant{% else %}Most Urgent Deadline{% endif %}
   </div>
   ```

   d. Update pagination links to preserve municipality param. Change all `/fund-your-fix?page=` hrefs to include municipality:
   ```html
   /fund-your-fix?page={{ page - 1 }}&per_page={{ per_page }}{% if municipality %}&municipality={{ municipality }}{% endif %}
   ```
   Apply this to Previous, Next, and numbered page links.

2. **Create `templates/fund_your_fix_widget.html`** as a STANDALONE HTML fragment (does NOT extend base.html):
```html
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>ADA Compliance Grants Widget</title>
<style>
  body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif; margin: 0; padding: 0.75rem; background: #fff; color: #333; font-size: 14px; }
  .widget-header { font-size: 1rem; font-weight: 700; color: #1a3a5c; margin-bottom: 0.75rem; }
  .grant-item { border-bottom: 1px solid #eee; padding: 0.5rem 0; }
  .grant-item:last-child { border-bottom: none; }
  .grant-title { font-weight: 600; color: #2563a8; text-decoration: none; display: block; margin-bottom: 0.25rem; }
  .grant-title:hover { text-decoration: underline; }
  .grant-meta { font-size: 0.8rem; color: #666; }
  .grant-meta span { margin-right: 0.75rem; }
  .widget-footer { margin-top: 0.75rem; text-align: center; }
  .widget-footer a { color: #2563a8; font-weight: 600; text-decoration: none; font-size: 0.85rem; }
</style>
</head>
<body>
<div class="widget-header">ADA Compliance Grants</div>
{% for opp in results %}
<div class="grant-item">
  <a class="grant-title" href="/fund-your-fix" target="_top">{{ opp.title }}</a>
  <div class="grant-meta">
    {% if opp.close_date %}<span>Closes: {{ opp.close_date }}</span>{% endif %}
    {% if opp.award_ceiling %}<span>Up to ${{ "{:,.0f}".format(opp.award_ceiling) }}</span>{% endif %}
  </div>
</div>
{% endfor %}
{% if not results %}
<p style="color: #999; text-align: center;">No ADA grants available.</p>
{% endif %}
<div class="widget-footer">
  <a href="/fund-your-fix" target="_top">View all grants &rarr;</a>
</div>
</body>
</html>
```
  </action>
  <verify>
    <automated>cd /home/jeff/Projects/grantflow && uv run python -c "
from pathlib import Path
html = Path('templates/fund_your_fix.html').read_text()
assert 'og:image' in html, 'Missing og:image'
assert 'municipality' in html, 'Missing municipality banner'
assert 'featured_is_fta' in html, 'Missing featured_is_fta conditional'
widget = Path('templates/fund_your_fix_widget.html').read_text()
assert 'base.html' not in widget, 'Widget must NOT extend base.html'
assert 'grant-item' in widget, 'Widget missing grant items'
print('All template checks passed')
"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'og:image' templates/fund_your_fix.html` matches `og-fund-your-fix.png`
    - `grep 'municipality' templates/fund_your_fix.html` shows the filter banner markup
    - `grep 'featured_is_fta' templates/fund_your_fix.html` shows the conditional label
    - `grep -c 'base.html' templates/fund_your_fix_widget.html` returns 0 (standalone, no extends)
    - `grep 'grant-item' templates/fund_your_fix_widget.html` confirms grant items rendered
    - `grep 'fund-your-fix' templates/fund_your_fix_widget.html` confirms link to full page
    - File templates/fund_your_fix_widget.html exists and contains `<!DOCTYPE html>`
  </acceptance_criteria>
  <done>og:image meta tag present, municipality banner shows when filter active, featured grant label changes for FTA, widget template is standalone HTML fragment with top grants and link to full page</done>
</task>

</tasks>

<verification>
Run full quality gate:
```bash
cd /home/jeff/Projects/grantflow && uv run ruff check . --fix && uv run ruff format . && uv run mypy grantflow/ && uv run pytest tests/test_fund_your_fix.py --tb=short -q
```
All existing tests must pass. New routes must be importable without errors.
</verification>

<success_criteria>
1. /fund-your-fix accepts ?municipality= param and filters grants with fail-open fallback
2. FTA "All Stations Access" grant is pinned as featured when present in database
3. /ada-grants returns HTTP 301 redirect to /fund-your-fix
4. /fund-your-fix/widget returns standalone HTML fragment with top 5 ADA grants
5. og:image meta tag present in fund_your_fix.html head
6. Municipality banner displays when filter is active
7. All existing tests pass, linter and type checker clean
</success_criteria>

<output>
After completion, create `.planning/phases/12-fund-your-fix-web-page-seo/12-01-SUMMARY.md`
</output>
