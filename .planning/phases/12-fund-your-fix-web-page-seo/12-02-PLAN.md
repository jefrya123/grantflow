---
phase: 12-fund-your-fix-web-page-seo
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - tests/test_fund_your_fix.py
autonomous: true
requirements:
  - ADA-04
  - ADA-05
  - ADA-06

must_haves:
  truths:
    - "Municipality filter test proves ?municipality= param works and fail-open returns all grants"
    - "/ada-grants redirect test proves 301 to /fund-your-fix"
    - "Widget endpoint test proves /fund-your-fix/widget returns 200 with grant titles"
    - "Featured FTA grant test proves All Stations Access is pinned when present"
    - "og:image test proves meta tag is in page HTML"
  artifacts:
    - path: "tests/test_fund_your_fix.py"
      provides: "Tests for municipality filter, redirect, widget, featured FTA, og:image"
      contains: "test_fund_your_fix_municipality_filter"
  key_links:
    - from: "tests/test_fund_your_fix.py"
      to: "grantflow/web/routes.py"
      via: "TestClient HTTP requests"
      pattern: "client.get"
---

<objective>
Add comprehensive tests for all Phase 12 new functionality: municipality filtering, /ada-grants redirect, widget endpoint, featured FTA grant pinning, and og:image meta tag.

Purpose: Verify all new web page features work correctly with automated tests.
Output: Updated tests/test_fund_your_fix.py with 6+ new test functions covering every gap item.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-fund-your-fix-web-page-seo/12-CONTEXT.md
@.planning/phases/12-fund-your-fix-web-page-seo/12-01-SUMMARY.md

<interfaces>
<!-- Test helper already in test_fund_your_fix.py -->
```python
def _make_ada_opp(
    db_session, opp_id, title="ADA Test Grant", close_date=None, **kwargs
):
    defaults = dict(
        id=opp_id,
        source_id=f"src-{opp_id}",
        title=title,
        source="grants_gov",
        opportunity_status="posted",
        opportunity_number=f"ADA-{opp_id}",
        topic_tags='["ada-compliance", "transportation"]',
    )
    if close_date:
        defaults["close_date"] = close_date
    defaults.update(kwargs)
    opp = Opportunity(**defaults)
    db_session.add(opp)
    db_session.commit()
    return opp
```

<!-- Routes added in Plan 01 -->
- GET /ada-grants -> 301 redirect to /fund-your-fix
- GET /fund-your-fix?municipality=<slug> -> filtered grants with banner
- GET /fund-your-fix/widget -> standalone HTML with top 5 grants
- fund_your_fix_page passes featured_is_fta and municipality to template
- FTA grant pinned via title ilike "%all stations%"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tests for municipality filter, redirect, widget, featured FTA, and og:image</name>
  <files>tests/test_fund_your_fix.py</files>
  <read_first>
    - tests/test_fund_your_fix.py (current tests and _make_ada_opp helper)
    - grantflow/web/routes.py (updated routes from Plan 01)
    - tests/conftest.py (client and db_session fixtures)
  </read_first>
  <behavior>
    - test_municipality_filter_shows_matching_grants: Create 2 grants, one with "boston" in eligible_applicants. GET /fund-your-fix?municipality=boston returns 200 and contains the matching grant title and "Showing grants for" banner text.
    - test_municipality_filter_fail_open: Create 1 grant with no municipality match. GET /fund-your-fix?municipality=nonexistent-city returns 200 and still shows the grant (fail-open).
    - test_ada_grants_redirect: GET /ada-grants returns 301 with Location header containing /fund-your-fix.
    - test_widget_returns_200: Create 2 ADA grants. GET /fund-your-fix/widget returns 200 with HTML containing both grant titles.
    - test_widget_no_base_html: GET /fund-your-fix/widget response does NOT contain base.html artifacts (e.g., the site nav). Contains "ADA Compliance Grants Widget" title.
    - test_widget_limits_to_5: Create 7 ADA grants. GET /fund-your-fix/widget returns exactly 5 grant-item elements.
    - test_featured_fta_grant_pinned: Create a grant titled "All Stations Access Program" with close_date far in future, and another grant with closer deadline. The featured section should show "All Stations Access" (not the closer-deadline grant).
    - test_og_image_present: GET /fund-your-fix returns HTML containing 'og:image' and 'og-fund-your-fix.png'.
  </behavior>
  <action>
Append the following test functions to `tests/test_fund_your_fix.py`. Use the existing `_make_ada_opp()` helper and `client`/`db_session` fixtures from conftest.py.

For each test:
- Create test data using `_make_ada_opp(db_session, ...)` with appropriate kwargs
- Use `eligible_applicants="City of Boston, MA"` for municipality filter test (so `?municipality=boston` matches via ilike)
- Use `description="Grant for Boston area transit"` as alternative match field
- For redirect test, use `client.get("/ada-grants", follow_redirects=False)` to capture the 301
- For widget tests, check `response.text` for `grant-item` class occurrences
- For featured FTA test, create "All Stations Access Program" with `close_date="2026-12-31"` and another grant with `close_date="2026-04-01"` -- the featured section should contain "All Stations Access" not the closer one
- For og:image test, just check `client.get("/fund-your-fix").text` contains `og:image` and `og-fund-your-fix.png`

Use unique `opp_id` values per test to avoid cross-test contamination (prefix with test name abbreviation, e.g., "muni-001", "redir-001", "widget-001", "fta-001", "og-001").
  </action>
  <verify>
    <automated>cd /home/jeff/Projects/grantflow && uv run pytest tests/test_fund_your_fix.py --tb=short -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def test_" tests/test_fund_your_fix.py` returns at least 13 (5 existing + 8 new)
    - `grep "municipality" tests/test_fund_your_fix.py` matches at least 4 lines (2 test functions + assertions)
    - `grep "ada-grants" tests/test_fund_your_fix.py` matches at least 1 line (redirect test)
    - `grep "widget" tests/test_fund_your_fix.py` matches at least 3 lines (3 widget tests)
    - `grep "all stations" tests/test_fund_your_fix.py` matches at least 1 line (featured FTA test, case-insensitive)
    - `grep "og.image" tests/test_fund_your_fix.py` matches at least 1 line
    - `grep "follow_redirects" tests/test_fund_your_fix.py` matches at least 1 line (redirect test)
    - `uv run pytest tests/test_fund_your_fix.py --tb=short -q` passes all tests with 0 failures
  </acceptance_criteria>
  <done>All 8 new tests pass: municipality filter (match + fail-open), /ada-grants 301 redirect, widget (200 + no base.html + limit 5), featured FTA pinning, og:image present</done>
</task>

</tasks>

<verification>
Run full quality gate:
```bash
cd /home/jeff/Projects/grantflow && uv run ruff check . --fix && uv run ruff format . && uv run mypy grantflow/ && uv run pytest tests/test_fund_your_fix.py -v --tb=short
```
All tests (existing + new) must pass. Linter and type checker clean.
</verification>

<success_criteria>
1. At least 8 new tests added to test_fund_your_fix.py
2. Municipality filter tests verify both matching and fail-open behavior
3. Redirect test confirms 301 status code and Location header
4. Widget tests verify content, standalone nature, and 5-item limit
5. Featured FTA test confirms "All Stations Access" is pinned over closer-deadline grants
6. og:image test confirms meta tag presence
7. All tests pass with 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/12-fund-your-fix-web-page-seo/12-02-SUMMARY.md`
</output>
